#include <stdio.h>
#include <math.h>
#include <stdlib.h>

// math functions = perform mathematical operations
// below are comments with examples of functions
// feel free to uncomment them when you want to see what they do
// top is int and bottom is floats
int main(){
    
    // int x = -3;
    // x = sqrt(x);
    // x = pow(x, 2);
    // x = abs(x);

    float x = 45;
    // x = round(x);
    // x = ceil(x);
    // x = floor(x);
    // x = log(x);
    // x = sin(x);
    // x = cos(x);
    x = tan(x);


    printf("%f\n",x);
    return 0;
}